import type { CodeChallengeMethod } from '../../types';
declare const createCodeChallengeAndVerifier: (length?: number, method?: CodeChallengeMethod) => Promise<{
    verifier: string;
    challenge: string;
}>;
export default createCodeChallengeAndVerifier;
