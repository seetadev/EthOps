import { IPFSResolver } from './types';
export default class DefaultIPFSResolver implements IPFSResolver {
    createURL: typeof DefaultIPFSResolver.defaultCreateUrl;
    static defaultCreateUrl(cid: string, path: string): string;
    constructor(createURL?: typeof DefaultIPFSResolver.defaultCreateUrl);
    resolve(uri: string): Promise<string>;
}
