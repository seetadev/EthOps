import { JRDDocument, WebFingerResolverOptions } from './types';
export default class DefaultWebFingerResolver {
    options: WebFingerResolverOptions;
    constructor(options: WebFingerResolverOptions);
    resolve(domain: string, user: string, rel: string, fallbackIssuer: string): Promise<JRDDocument>;
}
