import { IssuerResolver, IssuerResolverOptions } from './types';
export default class DefaultIssuerResolver implements IssuerResolver {
    options: IssuerResolverOptions;
    constructor(options: IssuerResolverOptions);
    resolve(username: string, fallbackIssuer: string): Promise<any>;
}
